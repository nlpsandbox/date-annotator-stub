{
    "devDependencies": {
        "@openapitools/openapi-generator-cli": "2.1.23"
    },
    "scripts": {
        "generate:server": "openapi-generator-cli generate -g python-flask -o server -i openapi.yaml",
        "lint": "cd server && flake8",
        "test": "cd server && tox"
    }
}
